document.addEventListener('DOMContentLoaded', function() {
  console.log('DOM carregado com sucesso.');
  window.mostrar = function(selecao) {
    console.log('Função mostrar chamada com:', selecao);
    // Lógica para mostrar/esconder divs
  };
  // Outras funções...
});